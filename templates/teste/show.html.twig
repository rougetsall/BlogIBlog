{% extends 'base.html.twig' %}

{% block title %}Teste{% endblock %}

{% block body %}
    <h1>Teste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ teste.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ teste.contenu ? teste.contenu|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('teste_index') }}">back to list</a>

    <a href="{{ path('teste_edit', {'id': teste.id}) }}">edit</a>

    {{ include('teste/_delete_form.html.twig') }}
{% endblock %}
